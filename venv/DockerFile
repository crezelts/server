# 1. 베이스 이미지 설정 (예: Python 3.12)
FROM python:3.12-slim

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. 로컬의 requirements.txt 파일을 컨테이너의 /app 디렉토리로 복사
COPY requirements.txt /app/

# 4. 필요한 패키지 설치
RUN pip install --no-cache-dir -r requirements.txt

# 5. 애플리케이션 코드 복사
COPY . /app/

# 6. 애플리케이션 실행 명령어
CMD ["python", "teramo.py"]
